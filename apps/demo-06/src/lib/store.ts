// store.ts — legacy file, all data now lives in data.ts
// This file is kept as a re-export for any remaining imports.

export { products, banners, graphicNav, brandAssets } from "./data";
export type { Product, Banner, Brand, Category } from "./data";
