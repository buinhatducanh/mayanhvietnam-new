{
  "buildCommand": "corepack enable && pnpm --filter demo-07 build",
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}