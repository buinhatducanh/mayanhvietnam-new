{
  "installCommand": "corepack enable && corepack prepare pnpm@10.34.5 --activate && pnpm install --no-frozen-lockfile --registry https://registry.npmjs.org/",
  "buildCommand": "pnpm --filter demo-10 build"
}
