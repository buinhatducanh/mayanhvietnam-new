{
  "buildCommand": "corepack enable && pnpm --filter demo-01 build",
  "installCommand": "corepack enable "installCommand": "corepack enable && pnpm install --frozen-lockfile""installCommand": "corepack enable && pnpm install --frozen-lockfile" pnpm install"
}