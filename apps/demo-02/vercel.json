{
  "buildCommand": "corepack enable && pnpm --filter demo-02 build",
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}